# find manuscripts by holding institution
SELECT ?ms ?msLabel ?institution ?institutionLabel
WHERE {
# bind query variables
  BIND(p:P47 AS ?holdingInstitutionAsRecorded).
  BIND(pq:P35 AS ?hasHoldingInstitution).
# statement: manuscript has holding institution recorded as literal string value
  ?ms ?holdingInstitutionAsRecorded ?institutionString .
# statement: string value has qualifier institution authority value
  ?institutionString ?hasHoldingInstitution ?institution .
  
  SERVICE wikibase:label { bd:serviceParam wikibase:language "[AUTO_LANGUAGE]". }
  }
ORDER by ?languageLabel
