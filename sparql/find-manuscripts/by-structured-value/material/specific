# find manuscripts with material type "X"
SELECT ?ms ?msLabel ?material ?materialLabel
WHERE {
# bind query variables
  BIND(wdt:P11 AS ?hasMaterial).
  BIND(wdt:P49 AS ?hasExternalURI).
# statement: manuscript has material type
  ?ms ?hasMaterial ?material .
# statement: material type represented by AAT value for "parchment"
  ?material ?hasExternalURI <http://vocab.getty.edu/page/aat/300011851>.
  
  SERVICE wikibase:label { bd:serviceParam wikibase:language "[AUTO_LANGUAGE]". }
  }
