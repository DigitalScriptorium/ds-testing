# find manuscript record associated with name "X"
SELECT ?record ?recordLabel ?name ?nameLabel ?nameType ?nameTypeLabel
WHERE {
# bind query variables
  BIND(p:P17 AS ?nameAsRecorded).
  BIND(pq:P31 AS ?hasName).
  BIND(wdt:P48 AS ?hasQID).
  BIND(wdt:P42 AS ?instanceOf).
# statement: manuscript record has associated name recorded as literal string value
  ?record ?nameAsRecorded ?nameString .
# statement: string value has qualifier name authority value
  ?nameString ?hasName ?name .
# statement: authority value has QID for "Ghazzālī, 1058-1111"
  ?name ?hasQID "Q9546"^^xsd:string .
# statement: authority value occurs in personal or corporate wikibase name authority 
  ?name ?instanceOf ?nameType
  
  SERVICE wikibase:label { bd:serviceParam wikibase:language "[AUTO_LANGUAGE]". }
  }
